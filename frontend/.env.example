# Base path for API calls. Left as a relative path in development so requests go
# through the Vite proxy and stay same-origin (required for httpOnly auth cookies).
VITE_API_BASE_URL=/api/v1

# Where the Vite dev proxy forwards /api to.
VITE_API_PROXY_TARGET=http://localhost:5050
